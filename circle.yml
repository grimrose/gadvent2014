machine:
  java:
    version: oraclejdk8
  timezone:
    Asia/Tokyo
  hosts:
    localhost: 127.0.0.1
  environment:
    DATABASE_URL: postgres://ubuntu:@127.0.0.1:5432/circle_test
    GADVENT2014_OPTS: -Dratpack.port=9000

dependencies:
  override:
    - ./gradlew dependencies

  cache_directories:
    - "~/.gradle/caches"

test:
  override:
    - ./gradlew test

deployment:
  staging:
    branch: master
    heroku:
      appname: quiet-dawn-6718
